# nvim Finder app

This installs `~/Applications/nvim.app`, a Finder wrapper that opens selected
files in Terminal with `nvim`.

```sh
./macos/finder-app/install.sh
```

The app is registered as the default editor for plain text, JSON, and Markdown.

